/**
 * Internal HTTP API authentication (Bearer + SHA-256 of api_keys).
 * Implementation: {@link verifyApiKeyRequest} in `@/lib/api/internal-auth`.
 */
export {verifyApiKeyRequest} from "@/lib/api/internal-auth";
